# GTM 環境変数サンプル
# 開発中は何も設定しなくても MOCK_MODE で全機能が動きます。

# モックモード（予約フラグ）。現状はこの値に関わらずモックで動作します。
# 実際のモック/本番の切替は「各サービスのキーの有無」で自動判定します
#   （例：STRIPE_SECRET_KEY があれば本物のStripe、無ければモック。DATABASE_URL があればDB接続）。
# 将来、データ源/LLM/検証APIの切替にこのフラグを使う予定です。
MOCK_MODE=true

# 既定の市場（JP または GLOBAL）
MARKET_DEFAULT=JP

# セッション署名用の秘密鍵（cookie改ざん防止）。本番は必ずランダムな長い文字列を設定
# 例: openssl rand -base64 32 で生成
AUTH_SECRET=

# ---- 外部データ/LLM/検証（最終フェーズで設定・それまで空でOK）----
# LLM_API_KEY=
# DATASOURCE_MAPS_API_KEY=
# DATASOURCE_JOBS_API_KEY=
# VERIFY_EMAIL_API_KEY=
# VERIFY_PHONE_API_KEY=

# ---- Stripe（コードは実装済み。鍵はクライアントが最後に設定）----
# STRIPE_SECRET_KEY=            # sk_test_... / sk_live_...
# STRIPE_WEBHOOK_SECRET=        # whsec_...（Webhook署名検証）
# STRIPE_PRICE_STARTER=         # price_...（Starterプランの価格ID）
# STRIPE_PRICE_PRO=             # price_...（Proプランの価格ID）
# STRIPE_PRICE_SCALE=           # price_...（Scaleプランの価格ID）

# ---- DB（クライアント用意の開発環境に接続。本番には触れない）----
# DATABASE_URL=                 # 例: postgresql://user:pass@host:5432/dbname?schema=public
# DIRECT_URL=                   # （任意）マイグレーション用の直結URL

# ---- AI（自然文の解釈。任意）----
# GEMINI_API_KEY=               # Google AI Studio(https://aistudio.google.com/) で無料発行できるAPIキー。
#                               #   設定すると、検索条件の読み取りにGeminiのAIを使います（未設定ならルールベースで動作）。
# GEMINI_MODEL=                 # 使うモデル名（省略時 gemini-2.5-flash）。無料枠で使えるモデルを指定。

# ---- 外部データ源（任意）----
# GBIZINFO_API_TOKEN=           # gBizINFO（経済産業省・無料の法人情報API）のトークン。
#                               #   設定すると日本市場の検索で公式の実在企業データを併用します。
#                               #   未設定ならモックのみで動作。取得先: https://info.gbiz.go.jp/
